<div class="post-card" style="border-left: none;">
  <h2 style="color: #667eea; margin-bottom: 15px;">👤 <%= @user.nickname %></h2>
  
  <div class="post-meta" style="margin-bottom: 25px;">
    <strong>💰 Баланс:</strong> 
    <span style="font-size: 1.3em; font-weight: bold; color: #28a745;">
      <%= number_to_currency(@user.balance, unit: "₴", separator: ",", delimiter: " ") %>
    </span><br>
    <strong>Створено:</strong> <%= @user.created_at.strftime("%d.%m.%Y о %H:%M") %><br>
    <% if @user.updated_at != @user.created_at %>
      <strong>Оновлено:</strong> <%= @user.updated_at.strftime("%d.%m.%Y о %H:%M") %>
    <% end %>
  </div>

  <div style="margin-top: 30px;">
    <h3 style="margin-bottom: 15px;">Перекази від цього користувача:</h3>
    <% sent = @user.sent_transfers.count %>
    <% if sent > 0 %>
      <p><strong>Відправлено переказів:</strong> <%= sent %></p>
    <% else %>
      <p style="color: #6c757d;">Ще не відправляв переказів</p>
    <% end %>

    <h3 style="margin-top: 20px; margin-bottom: 15px;">Перекази цьому користувачу:</h3>
    <% received = @user.received_transfers.count %>
    <% if received > 0 %>
      <p><strong>Отримано переказів:</strong> <%= received %></p>
    <% else %>
      <p style="color: #6c757d;">Ще не отримував переказів</p>
    <% end %>
  </div>
</div>

<div class="form-actions">
  <% if user_signed_in? && current_user.id == @user.id %>
    <%= link_to 'Редагувати профіль', edit_user_path(@user), class: 'btn btn-secondary' %>
  <% end %>
  <%= link_to 'Всі користувачі', users_path, class: 'btn btn-primary' %>
  <%= link_to 'Назад', root_path, class: 'btn btn-secondary' %>
</div>
